# TRNote 工程日志

> 规则：每实现一个功能，在根目录本文件追加一条记录，包含**改动内容 + 具体日期时分**。

---

## 2026-08-05 14:59 · 项目初始化

**改动：**
- 确认技术选型：Tauri v2（Rust 核心）+ React + Vite + SQLite + ECDICT 本地词库
- 确认开发环境：Node v18.20.8 / npm 10.8.2 / rustc 1.90.0
- 已完成需求文档（`需求文档.md`）与界面原型（`prototype/index.html`，双击可打开）
- 建立本工程日志，后续每个功能实现均在此记录

**状态：** 开始搭建项目骨架

---

## 2026-08-05 15:10 · 项目骨架 + Rust 后端核心

**改动：**
- 切换包管理器为 **pnpm**（用户偏好），并加入 `pnpm.onlyBuiltDependencies: ["esbuild"]` 白名单
- 搭建 Tauri v2 + React + Vite + TypeScript 项目骨架：
  - 根目录：`package.json` / `vite.config.ts` / `tsconfig*.json` / `index.html` / `.gitignore`
  - Tauri 侧：`src-tauri/Cargo.toml` / `tauri.conf.json` / `capabilities/default.json` / `main.rs` / `lib.rs`
  - 生成全套应用图标（`pnpm tauri icon`，源图 `icons/app-icon.png`）
- 实现 **SQLite 数据层**（`db.rs`）：words/senses/forms/settings 四表，含增删改查、全文检索、设置读写
- 实现 **本地词典引擎**（`dict.rs`）：ECDICT 只读库加载（数据目录 `ecdict.db`）+ 内存前缀索引（输入即出联想）+ 内置编程术语兜底词表
- 实现 **翻译服务**（`translate.rs`）：本地未命中时按顺序走免费 API（Google 非官方 → 有道 → 百度）
- 实现 **Git 自动同步**（`git_sync.rs`）：checkpoint → git init/remote → add/commit/push，失败可重试
- 实现 **备份恢复**（`backup.rs`）：基于 SQLite 在线备份 API 热备份/热恢复
- 实现 **系统集成**（`lib.rs`/`hotkey.rs`/托盘）：全局快捷键（默认 Ctrl+Alt+T，可自定义）、系统托盘（左键主界面/右键 主界面·设置·退出）、关闭隐藏到托盘、Toast 事件
- 实现 **命令层**（`commands.rs`）：14 个 Tauri 命令，前后端通信全通

**验证：** `cargo check` 通过（无编译错误）

---

## 2026-08-05 15:35 · React 前端 + 打包验证

**改动：**
- 实现 **React 前端**（`src/`）：三部分主界面（左列表 3/10 + 右上搜索框 + 右下词典式详情）
  - 实时查词交互：输入即本地联想（`lookup_local`）→ 停顿 400ms 本地无结果走网络（`lookup_word`）→ 确认后自动收录（`save_word`）
  - 组件：`App.tsx`（状态编排）/ `WordList` / `DetailView` / `SettingsWindow` / `ToastView`
  - 设置窗口：首次使用自动弹出、Git 仓库链接、快捷键自定义、自动收录/自动同步开关、API 兜底顺序**拖动排序**、备份/导入/立即同步
- 新增 `README.md`（使用说明）与 `scripts/build-ecdict.mjs`（ECDICT 词库转换脚本）
- 打包：`tauri.conf.json` 的 `bundle.targets` 改为 `["app"]`（跳过 MSI/WiX 联网下载，避免卡住）

**验证：**
- 前端 `tsc + vite build` 通过
- `pnpm tauri build` 成功，产出 `src-tauri/target/release/trnote.exe`（9.3MB）
- 冒烟测试：exe 启动后进程存活、无崩溃；`%APPDATA%\TRNote\trnote.db` 正常创建（后端初始化成功）

---

## 2026-08-05 16:05 · 排查"双击无窗口"问题

**现象：** 双击 `trnote.exe` 后无主界面弹出，但进程存活、数据库正常创建。

**排查过程：**
- 诊断脚本（`scripts/diagnose-window.ps1`）枚举窗口：主窗口已创建（1391x789）但 `Visible=False`，标题为空
- 确认 WebView2 运行时缺失：`Program Files (x86)\Microsoft\EdgeWebView`、Program Files、LOCALAPPDATA 均无 `msedgewebview2.exe`；无 Microsoft Edge

**结论：** 本机缺少 **WebView2 运行时**（Tauri 必需）。WebView2 初始化失败 → 窗口保持隐藏，导致"双击无界面但进程存活"。

**解决：** 需以管理员身份安装 WebView2 Evergreen 运行时：
```
winget install --id Microsoft.EdgeWebView2Runtime -e --accept-source-agreements --accept-package-agreements
```
或从 Microsoft 官网下载 Evergreen Standalone Installer，或安装 Microsoft Edge。

**经验：** 冒烟测试应验证窗口可见性而非仅进程存活；打包给其他机器时需考虑 WebView2 依赖（可用 NSIS 安装包内置 WebView2 引导安装）。

---

## 2026-08-05 16:22 · 迁移 Tauri → Electron（彻底摆脱 WebView2）

**背景：** 本机缺少可用 WebView2 运行时（残留 112 注册表项、文件缺失、安装器报错 0x80040708/0x80040828），Tauri 应用窗口无法显示。用户决定迁移到 Electron。

**改动（Electron + TS + pnpm，electron-vite）：**
- **删除**全部 Tauri/Rust 代码（`src-tauri/`、旧 `vite.config.ts`、旧 tsconfig、Tauri 依赖、Tauri 专用诊断脚本）
- 目录重构为 electron-vite 标准：`src/main`（主进程）/ `src/preload` / `src/shared`（共享类型）/ `src/renderer`（React 前端，原 `src/` 迁移）
- 主进程（TS）：`index.ts`（窗口/托盘/快捷键）、`ipc.ts`（IPC 命令层）、`db.ts`（better-sqlite3 数据层，从 Rust 移植）、`dict.ts`（ECDICT + 内置词表）、`translate.ts`（Google→有道→百度）、`gitSync.ts`（child_process git）、`backup.ts`（better-sqlite3 backup）、`hotkey.ts`、`window.ts`
- 渲染进程：`api.ts` 从 Tauri invoke 改为 preload 注入的 `window.api`（contextBridge）；组件/样式原样复用
- 配置：`.npmrc`（`node-linker=hoisted` + electron 镜像）、electron-vite 三端构建、electron-builder 打包（win dir 目标）
- 关键排错：
  - pnpm 默认拦截 postinstall → electron 二进制未下载；`pnpm.onlyBuiltDependencies` 未生效，改用 `node node_modules/electron/install.js` 手动下载
  - GitHub 直连下载 electron 极慢（ETA 24 分钟）→ `.npmrc` 配置 `electron_mirror=https://npmmirror.com/mirrors/electron/`，秒下
  - `?asset` 导入需 `electron-vite/node` 类型引用（`src/main/env.d.ts`）

**验证：**
- `pnpm typecheck` 通过（主进程 + 渲染进程）
- `pnpm build` 通过；`electron .` 直接运行：窗口出现（标题 TRNote、句柄有效、响应正常）
- 数据目录正常初始化（Chromium 缓存 + trnote.db 复用）
- `electron-builder --win` 打包成功 → `release/win-unpacked/TRNote.exe`（188MB，含 Chromium）；打包版 exe 冒烟测试通过，**无需 WebView2**

---

## 2026-08-05 16:28 · 修复：界面白屏（React error #310）

**现象：** Electron 版窗口能打开、标题正常，但界面内容空白。

**排查：** 带 `--enable-logging` 启动抓取渲染进程控制台报错：
```
Minified React error #310（Rendered fewer hooks than expected）
```

**根因：** `src/renderer/src/components/SettingsWindow.tsx` 中，`useState(dragging)` 被声明在 `if (!open) return null` 早退语句**之后**。`open` 从 `false`→`true` 时 Hook 数量变化，违反 React Hook 规则，导致整个应用卸载白屏。
（该 bug 在 Tauri 时代就存在，但因当时 WebView2 缺失从未成功渲染，未被发现；迁移到 Electron 后才暴露。）

**修复：** 把 `dragging` 状态提升到组件所有 Hook 之前（早退语句之前），并补充注释说明。

**验证：** 重新构建 + 打包后，`--enable-logging` 启动**无任何控制台报错**，窗口正常显示内容。

---

## 2026-08-05 16:34 · 安装 ECDICT 完整词库 + 词性解析改进

**背景：** 用户反馈"搜索 public 找不到"。排查发现：本地词库仅内置 40 个编程词，`ecdict.db` 未安装 → 打字实时联想失效；网络兜底其实正常（`public` 已被自动收录进库）。

**改动：**
- 安装 **ECDICT 完整词库**（skywind3000/ECDICT v1.0.28 预构建 `stardict.db`，811MB / 约 76 万词条）→ 复制到 `%APPDATA%\TRNote\ecdict.db`
  - 词库含 `stardict` 表（word/sw/phonetic/translation/exchange…），`stardict_3` 索引覆盖 `sw,word`
  - 验证：前缀联想 1ms、精确查询 0ms，打字即出
- 改进 `src/main/dict.ts` 的释义解析：支持 ECDICT 实际格式 `n. 公众, 民众`（英文词性缩写），兼容旧 `[名] 释义` 格式；词性映射补全（n./a./adj./ad./vt./vi./pron./prep.…）

**验证：** 构建打包后启动，窗口正常；词库查询秒出。

**经验：** "搜索无结果"要先区分是本地词库缺失还是网络失效；ECDICT 官方 release 提供预构建 SQLite 版（`ecdict-sqlite-28.zip`），无需自行转换。

---

## 2026-08-05 16:40 · 修复：本地联想崩溃（ECDICT null 字段）

**现象：** 装好 ECDICT 完整词库后，搜索仍然"没效果"（联想下拉不出结果）。

**排查：** 给词典模块加启动日志 + 启动自检（直接跑 `suggest`/`lookup`），抓到：
```
TypeError: Cannot read properties of null (reading 'split')
  at parseExchange
```
且日志确认词库加载成功（3,402,564 词条），路径 `%APPDATA%\trnote\ecdict.db`（Windows 大小写不敏感，与 TRNote 同目录）。

**根因：** ECDICT 词库中大量词条的 `exchange`（词形）、`translation` 字段为 **NULL**。`suggest()` 遍历联想结果时遇到 `exchange=null` → `parseExchange` 里 `null.split('/')` 崩溃 → 整个联想接口抛错 → 前端下拉永远为空。

**修复：** `dict.ts` 的 `parseEntry` 改为接受 `string | null`，对 null 字段用 `?? ''` 兜底；suggest/lookup 的类型标注同步修正。

**验证：**
- 自检通过：`suggest(pub)` 返回 `["pub","puba","pubarche"]`；`lookup(public)` 返回 `n. 公众, 民众` / `adj. 公众的...`（词性解析也正确）
- 移除临时自检代码；typecheck / build / 打包通过；应用启动正常

**经验：** 处理外部词典/数据集时，所有字段都要按可空处理（ECDICT 的 exchange/translation 常见 NULL）；加启动自检是定位"数据层静默失效"的有效手段。

---

## 2026-08-05 17:13 · 优化：收录新词后不再隐藏旧词

**现象：** 用户搜索并录入一个新单词后，左侧单词本只剩刚录入的词，**旧的单词被隐藏**。

**根因：** `App.tsx` 搜索框输入词会过滤左侧列表（筛选已收录）；**收录成功后搜索词仍保留**，`refreshList` 继续按搜索词过滤 → 旧词全部被隐藏。

**修复：** 收录完成（回车/点联想 `confirmSave`、或停顿自动收录）后，**清空搜索词并刷新完整列表**，同时选中刚收录的词展示详情。旧词始终可见。

**验证：** typecheck/build/打包通过；单实例启动正常（此前曾出现双实例导致快捷键被占用、缓存冲突，已清理）。

---

## 2026-08-05 17:17 · 修复：删除后输入框失效 + 列表始终显示完整词本

**问题（用户反馈）：**
1. 删除单词后，搜索输入框无法输入
2. 输入搜索词时，左侧词本会隐藏旧词

**根因：**
1. 删除用 **Electron 原生 `confirm()`** 弹窗，会抢占窗口焦点，导致删除后输入框失效
2. `App.tsx` 的 `refreshList` 按搜索词过滤列表，输入时旧词被隐藏

**修复：**
1. 改用**应用内自定义确认框**（`pendingDelete` 状态 + overlay 模态），彻底移除原生 `confirm()`；删除完成后主动 `inputRef.focus()` 聚焦输入框
2. `refreshList` **不再按搜索词过滤**，左侧列表始终展示完整词本；搜索框只负责本地联想 + 翻译收录；`onSearchChange`/`clearSearch` 移除多余刷新调用

**验证：** typecheck / build / 打包通过；单实例启动正常，窗口响应正常。

---

## 2026-08-05 17:19 · 新功能：联想下拉支持键盘选择

**需求：** 搜索时可以用键盘 ↑/↓ 选择词语。

**改动（`App.tsx` / `styles.css`）：**
- 新增 `suggestIndex` 高亮索引（-1=无）
- 搜索框键盘事件：`↑/↓` 在联想下拉中循环移动高亮（`preventDefault` 防止光标移动），`Enter` 优先确认高亮的联想词，否则按输入词收录
- 联想项高亮样式（`active` 与 hover 一致），鼠标悬停跟随高亮
- 联想建议更新/清空时重置高亮（`updateSuggests` 统一处理）

**验证：** typecheck / build / 打包通过；应用启动正常。











---

## 2026-08-05 17:22 · 左侧词本列表键盘导航

**改动：**
- 列表容器支持聚焦（`tabIndex=0`），点击任意词条后列表获得焦点
- `↑/↓` 键在词本中循环切换选中单词（到末尾后回到开头），右侧详情随之更新
- 选中变化时自动滚动到可见区域（`scrollIntoView block:'nearest'`）
- 去掉列表聚焦时的轮廓线（`outline:none`），视觉更干净

**验证：** typecheck / build / 打包通过；应用启动正常，窗口无报错。

---

## 2026-08-05 17:35 · 删除按钮醒目 + 圆角窗体 + 失焦自动隐藏

**改动：**
- 详情区删除按钮从图标改为红色醒目按钮（🗑 删除，hover 变实心红底白字）
- 去掉搜索框下方的 `search-hint` 提示行（顶部多余内容）
- 窗体改为无边框 + 透明 + CSS `border-radius:14px` 圆角（Win10 需透明方案才能圆角）
  - 新增顶部拖动条 + 侧栏标题区可拖动（`-webkit-app-region: drag`），排序按钮 `no-drag` 保持可点
  - `app.disableHardwareAcceleration()`：透明窗口在本机 GPU 上导致渲染进程崩溃，改软件渲染修复
  - 固定尺寸（`resizable:false`，透明窗口在 Windows 不可缩放）
- 失焦自动隐藏：窗口曾获得过焦点后，失焦即 `hide()`（弹窗式）；导出/导入原生对话框期间暂停（`blurSuppressed` 开关）
  - 需窗口先获得过焦点才生效，避免启动时被其它窗口抢焦点立即隐藏

**验证：** typecheck / build / 打包通过；截屏与 capturePage 确认圆角渲染正常、窗口可见、失焦后自动隐藏。

---

## 2026-08-05 17:49 · 移除失焦自动隐藏 + ESC 快捷键关闭 + 修复呼出闪烁

**改动：**
- 移除「失去焦点自动隐藏主窗口」功能（原 blur 处理器、focusedOnce 标志、blurSuppressed 暂停开关全部删除）
- 新增 ESC 收起主窗口：渲染进程监听 `Escape` → 调用 `hide_window` IPC → 主窗口隐藏到托盘
  - 设置弹窗打开时 ESC 先关闭设置；删除确认框打开时 ESC 先取消删除
- 修复快捷键呼出主窗口时的闪烁：原因是被隐藏时 Chromium 节流了渲染（backgroundThrottling 默认开启），呼出瞬间需重新绘制导致闪一下；设为 `backgroundThrottling:false` 后隐藏期间持续渲染，呼出不再闪烁

**验证：** typecheck / build / 打包通过；确认窗口在失焦后保持可见（不再自动隐藏）、产物含 hide_window/backgroundThrottling 配置。

---

## 2026-08-05 17:52 · 彻底消除呼出闪烁（透明窗口淡入）

**改动：**
- 上一版 backgroundThrottling:false 只解决渲染线程部分，透明窗口在 Windows 直接 show 时 DWM 合成首帧偶尔延迟仍会闪
- `showMain()`：窗口隐藏时先 `setOpacity(0)` 再 show/focus，60ms 后淡入到 `setOpacity(1)`，窗口不会以未合成状态出现；已可见时直接聚焦不做淡入
- 初始启动也统一走该逻辑：BrowserWindow 设 `show:false`，`ready-to-show` 后调用 `showMain()` 淡入显示

**验证：** typecheck / build / 打包通过；启动后主窗口正常显示（vis=True）、无崩溃。

---

## 2026-08-05 17:54 · 修复淡入方案：弃用 setOpacity 改用 CSS 淡入

**改动：**
- 上一版用 Electron `win.setOpacity(0)`→show→`setOpacity(1)` 在 Windows 上对透明窗口不可靠：隐藏时设置透明度，show 后卡在 opacity 0，窗口完全透明不可见
- 撤销 setOpacity 方案（`showMain` 恢复为直接 show/focus，窗口始终 opacity 1）
- 改用**纯 CSS 淡入**：`.app.fade-in` 播放 0.18s 的 opacity 0→1 动画；渲染进程监听 `visibilitychange`，窗口隐藏时移除该类、显示时重新添加触发动画——每次呼出（快捷键/托盘/启动）都有真正的淡入，且能盖住 DWM 首帧闪烁
- 保留 backgroundThrottling:false 与 ready-to-show（内容就绪后再显示）

**验证：** typecheck / build / 打包通过；窗口可见（1000x680）、中心像素为不透明内容（非空白透明）。

---

## 2026-08-05 18:02 · 左右方向键切换输入框与左侧单词本

**改动：**
- 搜索框按 `←` 聚焦左侧单词本；单词本按 `→` 回到搜索框（纯键盘切换两处）
- `WordList` 列表容器 ref 提升到 App 持有（`listRef` prop），并新增 `onMoveRight` 回调切回输入框
- ↑/↓ 在单词本内仍用于切换单词；空列表时 `→` 也能切回输入框

**验证：** typecheck / build / 打包通过；应用启动正常无报错。

---

## 2026-08-05 18:06 · 排序更清晰 + 左右栏比例随焦点切换（带动画）

**改动：**
- 排序方式更明确：新增「排序」标签，按钮改为「最新添加」「字母顺序」+ 等宽布局 + 高亮阴影
- 左右栏比例随焦点切换：新增 focusSide 状态（输入框 onFocus→right，词本列表 onFocus→left）
  - 焦点在右侧：侧栏 24%（右侧详情占比大）
  - 焦点在左侧：侧栏 42%（词本占比大）
  - `.sidebar` 加 `transition: width 0.28s`，←/→ 切换时左右栏平滑伸缩，焦点切换一目了然
- WordList 新增 onFocusList 回调通知 App 焦点所在侧

**验证：** typecheck / build / 打包通过；截屏确认窗口可见、默认（焦点右）侧栏宽约 24%（分界 x≈239）。

---

## 2026-08-05 18:09 · 输入框为空时 ↑/↓ 直接操作左侧词本

**改动：**
- 搜索框为空且有焦点时，按 `↑/↓` 不再只做联想导航（空查询无联想），而是**切到左侧词本并移动选中单词**（新 `moveListSelection`：算下一个/上一个词 → selectWord → 聚焦列表）
- 聚焦列表后自动触发左右栏比例动画（focus-left），后续 `↑/↓` 继续在词本内切换
- 输入框有内容时 `↑/↓` 仍为联想导航；`←/→`/`Enter` 行为不变

**验证：** typecheck / build / 打包通过；应用启动正常无报错。

---

## 2026-08-05 18:17 · 优化包体体积（280MB → 232MB）

**改动：**
- 新增 `scripts/after-pack.js`（electron-builder afterPack 钩子）：
  - 删除多余语言包：仅保留 zh-CN / zh-TW / en-US（55 个 → 3 个，省 ~39MB）
  - 删除 better-sqlite3 自带 sqlite 源码 deps/（运行时只用编译好的 .node，省 ~8.8MB）
- package.json 构建配置加 `compression: maximum` + `afterPack`

**效果：** win-unpacked 280.1MB → 231.7MB（省 ~48MB，17%）；主体仍是 Electron 运行时 TRNote.exe（180MB，无法再压，除非改 NSIS/zip 压缩安装包）。

**验证：** 打包通过、afterPack 生效；精简后应用启动正常、词典加载正常、无崩溃。

---

## 2026-08-05 18:21 · NSIS 压缩安装包（分发体积 232MB → 71.9MB）

**改动：**
- 新增 `build:installer` 脚本（`electron-builder --win nsis` + 内置 npmmirror 镜像环境变量）
- 修复 NSIS 构建报错：build 配置加 `publish: null`（跳过无仓库时的更新信息生成）
- 产物：`release/TRNote Setup 0.1.0.exe`（71.9MB，一次性安装程序，用户目录免管理员安装 + 开始菜单快捷方式）；绿色目录 `win-unpacked`（232MB）保留供日常直接运行

**效果：** 分发体积 232MB → 71.9MB（压缩 69%）；日常开发仍用 `build:win` 生成绿色目录。

**验证：** `pnpm run build:installer` 干净退出（exit=0）；精简后绿色版启动正常、窗口正常、词典加载正常。

---

## 2026-08-05 18:24 · 卸载误装版本 + 安装包改为手动安装向导（不再默认静默安装）

**改动：**
- 卸载误装到 %LOCALAPPDATA%\Programs\trnote 的版本：杀进程 → 静默运行 Uninstall TRNote.exe → 删除残留空目录与开始菜单快捷方式、注册表卸载项；数据目录 %APPDATA%\trnote 完整保留
- 修改安装行为：NSIS 配置 `oneClick:false` + `allowToChangeInstallationDirectory:true` —— 安装包运行后显示向导让用户**自选安装目录**，不再一键静默装到默认位置；桌面快捷方式默认不创建，仅开始菜单
- 重新构建安装包（71.9MB，exit=0）

**验证：** 卸载干净（目录/注册表/快捷方式均已清除）；绿色目录版（release/win-unpacked/TRNote.exe）启动正常、词典加载正常。

---

## 2026-08-05 18:31 · 更新 README + 设置界面加入快捷键说明

**改动：**
- README.md 修正：删除残留的 Tauri 命令（tauri dev/build），改为 electron-vite 命令；新增「⌨️ 键盘快捷键」章节（↑↓←→ / Enter / ESC / Ctrl+Alt+T）；更新快速开始（绿色免安装版 + 安装包向导两种方式）；补充 build:installer 构建命令
- 设置界面新增「方向键 & 快捷键说明」区块（←/→ 切换、↑/↓ 词本/联想、Enter、ESC），kbd 键帽样式

**验证：** typecheck / build / 打包通过；应用启动正常。

---

## 2026-08-05 18:37 · 升级 exe 图标为高清多尺寸 ICO

**改动：**
- 原 resources/icon.ico 仅 32x32（打包确实会嵌入 exe，但大图标显示模糊）
- 新增 `scripts/gen-icon.ps1`：从 512x512 的 icon.png 生成多尺寸 icon.ico（256/128/64/48/32/16，PNG 压缩存储）
- 重新打包：electron-builder 将多尺寸 ico 嵌入 TRNote.exe，桌面/任务栏/Alt+Tab 大图标清晰
- 说明：ExtractAssociatedIcon 返回 32x32 是 Windows API 默认小图标行为，不影响 exe 已内嵌多尺寸

**验证：** icon.ico 解析含 6 个尺寸（256 最大）；重新打包成功；应用启动正常。

---

## 2026-08-05 18:52 · 设置界面新增「下载词库」功能

**改动：**
- 新增 `src/main/dictDownload.ts`：从国内可达镜像下载 ECDICT 压缩包（ecdict-sqlite-28.zip，206.7MB）→ Win10 自带 tar/bsdtar 解压 → 找到 stardict.db → 覆盖安装到 %APPDATA%\trnote\ecdict.db → 重新加载词典（无需重启）
  - 下载源：ghfast.top → gh-proxy.com → ghproxy.net → GitHub 官方（镜像优先，境外直连可用 GitHub）
  - Node https 下载自动跟随重定向、按 Content-Length 报进度；失败自动切换下一源
- `dict.ts`：新增 `close()`/`closeDict()`/`reloadDict()`（覆盖安装前关闭旧连接避免 Windows 文件占用，装完热加载）
- 设置界面新增「本地完整词库（ECDICT）」区块：显示已/未安装状态（含大小）、「下载词库/重新下载词库」按钮、进度条 + MB 进度、完成/失败提示（Toast）
- IPC：`dict_status`（查状态）、`download_dict`（下载+进度事件 dict-progress）
- 共享类型/API/preload 同步扩展

**验证：** typecheck/build/打包通过；实测完整管线：ghfast.top 下载 206.7MB → tar 解压出 stardict.db（811.9MB）→ better-sqlite3 打开、stardict 表 340 万词条可用；应用启动正常。

---

## 2026-08-06 09:35 · 设置面板新增「打开词典文件夹」与「删除词库」

**改动：**
- 设置「本地完整词库」区块新增两个按钮：
  - 「打开词典文件夹」：shell.openPath 打开 %APPDATA%\trnote（数据目录）
  - 「删除词库」：两步确认（再点一次变红色「确认删除？」+ 取消）→ 主进程先 closeDict 关连接再删 ecdict.db(-shm/-wal)，initDict 恢复内置小词表，Toast 提示释放空间
- IPC：`open_dict_folder`、`delete_dict`；共享类型/API/preload 同步扩展；新增 .btn.danger / .btn-danger 样式

**验证：** typecheck / build / 打包通过；应用启动正常、词典加载正常。

---

## 2026-08-06 09:37 · 左侧词本 Del 键删除单词

**改动：**
- 焦点在左侧词本时按 `Del` 删除当前选中的单词：WordList 新增 `onDelete` 回调 + handleKeyDown 处理 Delete 键，复用 App 的 requestDelete（两步确认弹窗，防误删）
- 设置「方向键 & 快捷键说明」新增一行：`Del` 删除左侧词本当前选中的单词

**验证：** typecheck / build / 打包通过；应用启动正常、词库加载正常。

---

## 2026-08-06 09:40 · 删除单词后自动切到最新单词

**改动：**
- confirmDelete 删除后不再清空详情，而是自动选中「最新收录的单词」：当前为「最新添加」排序时直接用刷新列表第一个；「字母顺序」排序时也按 listWords('new') 取最新添加的第一个
- 词本为空时仍清空详情；保留删除后聚焦输入框的行为

**验证：** typecheck / build / 打包通过；应用启动正常、词库加载正常。

---

## 2026-08-06 09:44 · 设置窗口：仅内容区滚动，标题与底部按钮固定

**改动：**
- .settings 改为 flex 纵向布局（overflow hidden），内容包进 .settings-body 单独 overflow-y:auto 滚动
- 「设置」标题 + 右上角 ✕ 固定不滚动；底部「关闭/保存」按钮也固定在底部（不再随内容滚走）
- .settings-foot 由 margin-top 改为自带底部 padding（padding: 14px 0 20px）

**验证：** typecheck / build / 打包通过；应用启动正常。

---

## 2026-08-06 09:51 · 词库便携化：存放位置改为安装目录

**改动：**
- `src/main/dict.ts`：新增 `dictDataDir()` = `app.isPackaged ? dirname(app.getPath('exe')) : app.getPath('userData')`；`initDict()`/`getDict()`/`reloadDict()` 改为无参，内部用 `dictDataDir()`
- `src/main/dictDownload.ts`：下载/安装/删除词库目标目录改为 `dictDataDir()`，移除 `electron.app` 依赖
- `src/main/ipc.ts`：`dict_status`/`open_dict_folder`/`delete_dict` 改用 `dictDataDir()`；删除 electron.app 导入
- `src/main/index.ts`：`initDict()` 无参调用
- 生词本 `trnote.db` 仍存 `%APPDATA%`（`db.ts` 不变）；仅完整词库 ECDICT 移到安装目录
- 效果：绿色目录拷走即带词库（便携），实时下载进度条不变

**验证：** typecheck + build 通过；electron-builder 打包后启动，日志确认 `[dict] ECDICT 词库已加载: G:\TRNote\release\win-unpacked\ecdict.db（3402564 词条）`

---

## 2026-08-06 09:57 · 删除单词确认面板开关（默认关闭）

**改动：**
- `src/shared/types.ts`：`Settings` 新增 `confirmDelete: boolean`
- `src/main/db.ts`：`loadSettings` 默认 `confirmDelete:false`（默认不开启），key=`confirm_delete`；`saveSettings` 持久化
- `src/renderer/src/components/SettingsWindow.tsx`：新增「删除单词确认」开关（desc：关闭后直接删除）
- `src/renderer/src/App.tsx`：删除逻辑拆分 `doDelete(word)`（执行删除+自动切最新词）；`requestDelete` 按设置分流——开启则 `setPendingDelete` 弹确认框，关闭则直接 `doDelete` 不弹框；`confirmDelete` 从确认框触发后调 `doDelete`
- 默认关闭：删除单词直接执行，不弹确认面板

**验证：** typecheck + build 通过；electron-builder 重新打包，启动窗口正常（重新打包已清掉安装目录词库，处于未下载状态，可用于测试下载词库）

---

## 2026-08-06 10:10 · 多语言支持（中英文切换 + 可扩展语言包）

**改动：**
- `src/shared/i18n.ts`（新增）：`Lang`/`Translation` 类型 + `zh`/`en` 语言包 + `getText`/`makeT` + `SUPPORTED_LANGS`/`LANG_NAMES`；文件头注释写明「添加新语言 3 步」（复制 zh 翻译 → 加进 translations → 补 Lang/LANG_NAMES/SUPPORTED_LANGS），设置下拉自动出现新语言
- `src/shared/types.ts`：`Settings` 新增 `lang: Lang`（默认 zh）；`db.ts` 读写 `lang` 键
- 渲染进程：新增 `src/renderer/src/i18n.tsx`（I18nProvider + useT）；App.tsx 用 `makeT` + 包裹 Provider，WordList/DetailView/ToastView/SettingsWindow 全部文案改 `t()`；SettingsWindow 新增「界面语言」下拉（遍历 SUPPORTED_LANGS）
- 主进程：ipc.ts 的 toast/错误/对话框标题消息、dictDownload.ts 进度与结果消息、translate.ts 兜底错误、托盘菜单全部走 `makeT(db.loadSettings().lang)`
- 托盘抽离为 `src/main/tray.ts`：`createTray`/`refreshTray`，保存设置后 `refreshTray()` 重建菜单（语言切换即时生效）

**验证：** typecheck + build 通过；重新打包；DB 设 lang=en 重启应用正常（英文语言包加载无崩溃，截屏留档 scripts/shot-en.png）；已恢复 zh。发现：打包 exe 与开发 electron.exe 的 ELECTRON_RUN_AS_NODE 均不生效，改 DB 用系统 sqlite3.exe（D:\platform-tools\sqlite3.exe）

---

## 2026-08-06 10:15 · 设置界面：全部选项即时生效 + 语言单选框 + 移除保存/关闭按钮

**改动：**
- `SettingsWindow.tsx`：
  - 语言从下拉 select 改为**单选框**（遍历 SUPPORTED_LANGS，切换即生效并持久化）
  - 新增 `commit(partial)`：任意选项变更立即 `onSave`（即时生效，无需点保存）；文本输入（Git 仓库/hotkey）在 **onBlur** 时提交
  - 开关（自动收录/自动同步/删除确认）、语言单选、API 顺序拖动结束均即时保存
  - **移除底部「关闭/保存」按钮**；关闭仅用右上角 ✕
  - `useEffect` 同步依赖改为 `[open]`（仅打开时同步一次，App 更新 settings 不回写表单，避免覆盖正在编辑的内容）
- `App.tsx`：`handleSaveSettings` → `handleApplySettings`（保存 DB + 刷新 state，**不关闭窗口、不弹 toast**）
- `styles.css`：新增 `.lang-options`/radio 单选框样式；`.settings` 底部留白调整

**验证：** typecheck + build 通过；重新打包；启动窗口正常（重新打包再次清掉安装目录词库）

---

## 2026-08-06 10:18 · 主界面悬浮卡片阴影（体积感 + 防混白底）

**改动：**
- `styles.css` `.app`：高度改 `calc(100vh - 28px)` + `margin: 14px`（四周留透明边容纳阴影）；加 4 层半透明 `box-shadow`（1px 贴边 + 4px/16px 中远柔和 + 1px 细描边），形成悬浮卡片体积感，避免与白色/浅色背景混为一体
- 原理：透明窗口内 .app 是唯一不透明卡片，阴影在 margin 透明区内渲染；圆角与拖动条（.drag-strip fixed 顶部）、overlay 遮罩均不受影响

**验证：** typecheck + build 通过；重新打包启动窗口正常；截屏留档 scripts/shot-shadow.png

## 2026-08-06 10:21 · 阴影减淡调整

**改动：** `styles.css` `.app` box-shadow 缩为原先约 1/3（40px→20px、16px→8px、透明度 0.16→0.08，去掉 1px 细描边），保留 14px 圆角（阴影沿圆角渲染为柔和圆角投影）

**验证：** build + 重新打包 + 启动窗口正常

## 2026-08-06 10:24 · 弹窗遮罩只覆盖主体（不含阴影边）

**改动：** `styles.css`：`.app` 加 `position:relative`（定位基准）；`.overlay` 由 `position:fixed; inset:0`（全窗口）改为 `position:absolute; inset:0`（相对 .app 主体），配合 .app overflow:hidden 随圆角裁剪——半透明黑底只盖主界面卡片，阴影 margin 区保持透明不被压暗

**验证：** build + 重新打包 + 启动窗口正常

## 2026-08-06 10:27 · 底部设置/同步按钮上移

**改动：** `styles.css` `.footer-actions` bottom 16px→30px（卡片底部有 14px 阴影透明边，修正后距卡片底约 16px，不再贴底）

**验证：** build + 重新打包 + 启动窗口正常

## 2026-08-06 10:29 · 底部设置/同步按钮右移

**改动：** `styles.css` `.footer-actions` left 20px→28px（右移 8px，卡片左侧 14px 阴影透明边修正后距卡片左缘约 14px）

**验证：** build + 重新打包 + 启动窗口正常

## 2026-08-06 10:41 · 更换应用图标并重新打包

**改动：** 用户更换 `resources/icon.png`（10:36）；运行 `scripts/gen-icon.ps1` 从新图标重新生成多尺寸 `resources/icon.ico`（256/128/64/48/32/16，12KB，10:40）；electron-builder 重新打包，exe 内嵌新图标（10:41）

**验证：** 启动窗口正常；exe 图标已更新（ExtractAssociatedIcon 导出留档 scripts/exe-icon.png）

## 2026-08-06 10:48 · 图标重新打包（使用用户提供的完整多分辨率 ICO）

**改动：** 用户已手动准备好 `resources/icon.ico`（1.5MB，含 10 个分辨率 16/32/48/64/72/80/96/128/256/256）。本次**不再运行 gen-icon.ps1 覆盖**，直接以该 icon.ico 打包，exe 内嵌完整多分辨率图标（10:48）

**备注：** 上一次（10:40）误跑 gen-icon.ps1 从 icon.png 生成了仅 6 尺寸、12KB 的小 ICO 覆盖了用户图标，已纠正；后续换图标默认直接用 icon.ico，不自动重新生成
